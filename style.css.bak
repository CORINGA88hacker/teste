*{box-sizing:border-box;font-family:Arial,Helvetica,sans-serif}
body{margin:0;background:#111;color:#eee;display:flex;align-items:center;justify-content:center;height:100vh}
#game{width:900px;max-width:95%;background:#0f1724;border-radius:10px;overflow:hidden;box-shadow:0 10px 30px rgba(0,0,0,.7)}
#scene{position:relative;height:450px;background:#222}
#sceneImg{width:100%;height:100%;object-fit:cover;display:block}
#charactersLayer{position:absolute;left:0;right:0;top:0;bottom:0;pointer-events:none}
.char{position:absolute;bottom:10px;width:150px;pointer-events:auto;cursor:pointer;transition:transform .2s}
.char:hover{transform:translateY(-10px)}
#ui{padding:12px;background:#071024;display:flex;gap:12px;align-items:flex-start}
#dialogBox{flex:1}
#dialogText{min-height:48px;padding:8px;border-radius:6px;background:rgba(255,255,255,0.03)}
.choiceBtn{display:block;margin-top:8px;padding:8px;border-radius:6px;background:#0b6;cursor:pointer;color:#012}
.choiceBtn:hover{opacity:.9}
#controls{width:240px;text-align:right}
#nextScenario{padding:8px 12px;border-radius:6px;background:#285;cursor:pointer}
.overlay{position:absolute;inset:0;background:rgba(3,7,18,0.9);display:flex;flex-direction:column;align-items:center;justify-content:center;z-index:10}
.overlay input{padding:8px;margin:8px;border-radius:6px;width:220px}


/* TTS controls and history */
.ttsControls { display:flex; gap:8px; align-items:center; flex-wrap:wrap; margin-bottom:8px; }
.dialogHistory { max-height:140px; overflow:auto; border:1px solid rgba(0,0,0,0.1); padding:6px; background:rgba(255,255,255,0.9); margin-bottom:8px; }
.histItem { font-size:13px; margin:2px 0; }
